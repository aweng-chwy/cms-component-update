interface:
  display_name: "CMS Component Update"
  short_description: "Update CMS components for new fields."
  default_prompt: "Use $cms-component-update to update the CMS component for these authoring changes."
